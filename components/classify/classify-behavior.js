// 定义behavior 行为   和组件基本相同
let classifyBehavior = Behavior({

    // 组件共同的属性
    properties: {
        img :String,
        content:String
    },

    data:{

    },

    methods:{

    }
});

export {classifyBehavior}